server:
  port: 9092
  error:
    include-message: always

spring:
  application:
    admin: user
  datasource:
    url: jdbc:postgresql://localhost:50005/user
    username: "user-root"
    password: "password"
    jpa:
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          format_sql: "true"
      hibernate:
        ddl-auto: update
      show-sql: "true"